# Vercel Environment Variables Template
# Add these to your Vercel project settings

# ==========================================
# BACKEND ENVIRONMENT VARIABLES (Vercel)
# ==========================================

# MongoDB Connection String
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your_super_secure_random_jwt_secret_here_generate_with_crypto
JWT_SECRET_EXPIRY=7d

# Environment
NODE_ENV=production

# CORS Frontend URL (must match frontend deployment URL)
FRONTEND_URL=https://your-frontend-domain.vercel.app

# Server Port (Vercel auto-assigns, keep for reference)
PORT=3001

# ==========================================
# FRONTEND ENVIRONMENT VARIABLES (Vercel)
# ==========================================

# API URL (must match backend deployment URL)
VITE_API_URL=https://your-backend-domain.vercel.app

# ==========================================
# SETUP INSTRUCTIONS
# ==========================================

# 1. MongoDB Atlas Setup:
#    - Go to https://www.mongodb.com/cloud/atlas
#    - Create a cluster
#    - Create a user with password
#    - Whitelist 0.0.0.0/0 for Vercel IPs
#    - Copy connection string and replace username:password

# 2. JWT Secret Generation:
#    - Run in your terminal: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#    - Copy the output and use as JWT_SECRET

# 3. Vercel Deployment:
#    - Deploy backend first, get its URL
#    - Deploy frontend, set VITE_API_URL to backend URL
#    - Set FRONTEND_URL in backend to frontend URL

# 4. Add to Vercel:
#    - Go to Project Settings > Environment Variables
#    - For BACKEND: Add all MONGODB_URI, JWT_SECRET, JWT_SECRET_EXPIRY, NODE_ENV, FRONTEND_URL
#    - For FRONTEND: Add VITE_API_URL

# ==========================================
# SECURITY NOTES
# ==========================================

# - NEVER commit real environment variables to git
# - NEVER share your JWT_SECRET
# - Use strong MongoDB passwords (20+ characters)
# - Regenerate secrets if they leak
# - Keep .env files in .gitignore
# - This file is just a template - it's safe to commit

